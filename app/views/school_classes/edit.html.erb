<%= form_for @school_class, :url => school_class_path(@school_class) do |f| %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :room_number %>
    <%= f.number_field :room_number %>

    <%= f.submit "Update School class" %>

<% end %>